{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core webview access plus notifications, so people are told when they are spoken to.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default"
  ]
}
